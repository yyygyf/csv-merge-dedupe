# CSV 数据体检与客户验收报告

本地检查 CSV，生成无需联网即可打开的中文 HTML 报告。适用于导入前检查、订单表验收、重复客户编号排查。由 Codex 辅助开发，提供测试；不是客户项目或收入证明。

要求 Node.js 20 或更新版本，无第三方依赖。下载本仓库后运行：

```sh
node audit.mjs input.csv report.html --required id,name,amount --key id --numbers amount
node --test merge.test.mjs audit.test.mjs
```

- `--required`：空字符串及纯空白为缺失。
- `--key`：支持逗号分隔的组合键；精确文本匹配，001 与 1 不同，不自动裁剪空格。空键单独报告。
- `--numbers`：非空值须为有限十进制数（允许科学记数法），不接受货币符号、千分位和 NaN。空值交给必填规则。
- 报告显示异常总数、分类计数和最多 100 条异常记录编号，不包含原始单元格内容。列名仍会显示，分享前请检查。
- 输出文件已存在时拒绝覆盖；不修改输入文件。报告生成成功返回退出码 0，即使发现数据问题；运行错误返回 1。

范围：UTF-8、逗号分隔 CSV、内存处理。不支持 XLSX、GBK、超大流式文件、业务真伪判断或任意自定义公式。字段数不匹配的记录只统计结构问题。命令行列名列表不支持列名本身含逗号。

## 可委托定制

可讨论字段映射、业务规则、批量报告及与原有系统导入流程衔接。小范围工具定制参考 199–499 元；先确认范围、样本、期限和付款条件，再确定报价。请通过本仓库 Issues 提交脱敏或合成样本和验收需求，不要上传客户隐私数据、账号或密钥。

本工具和 `merge.mjs` 配合可完成“先体检，再合并去重，再体检”的本地数据处理流程。
